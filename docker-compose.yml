version: '3.8'

services:
  vulnerable-app:
    build: ./vulnerable-app
    ports:
      - "8080:80"
    networks:
      - security-lab
    container_name: vulnerable-web

  security-tools:
    build: ./security-tools
    networks:
      - security-lab
    container_name: security-tools
    stdin_open: true
    tty: true

networks:
  security-lab:
    driver: bridge